<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.APP1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orchid</item>
    </style>

    <style name="Theme.APP1" parent="Base.Theme.APP1" />

    <style name="Theme.APISplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/orchid</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icono_pelicula</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.APP1</item>
    </style>
</resources>